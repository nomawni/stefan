<form method="post" action="{{ path('comments_reply_rating_delete', {'id': comments_reply_rating.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comments_reply_rating.id) }}">
    <button class="btn">Delete</button>
</form>
