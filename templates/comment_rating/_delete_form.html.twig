<form method="post" action="{{ path('comment_rating_delete', {'id': comment_rating.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comment_rating.id) }}">
    <button class="btn">Delete</button>
</form>
