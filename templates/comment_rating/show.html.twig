{% extends 'base.html.twig' %}

{% block title %}CommentRating{% endblock %}

{% block body %}
    <h1>CommentRating</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comment_rating.id }}</td>
            </tr>
            <tr>
                <th>Action</th>
                <td>{{ comment_rating.action }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('comment_rating_index') }}">back to list</a>

    <a href="{{ path('comment_rating_edit', {'id': comment_rating.id}) }}">edit</a>

    {{ include('comment_rating/_delete_form.html.twig') }}
{% endblock %}
