{% extends 'base.html.twig' %}

{% block title %}Star{% endblock %}

{% block body %}
    <h1>Star</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ star.id }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ star.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('star_index') }}">back to list</a>

    <a href="{{ path('star_edit', {'id': star.id}) }}">edit</a>

    {{ include('star/_delete_form.html.twig') }}
{% endblock %}
