{% extends 'base.html.twig' %}

{% block title %}WhishLists{% endblock %}

{% block body %}
    <h1>WhishLists</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ whish_list.id }}</td>
            </tr>
            <tr>
                <th>DateAdded</th>
                <td>{{ whish_list.dateAdded ? whish_list.dateAdded|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('whish_lists_index') }}">back to list</a>

    <a href="{{ path('whish_lists_edit', {'id': whish_list.id}) }}">edit</a>

    {{ include('whish_lists/_delete_form.html.twig') }}
{% endblock %}
